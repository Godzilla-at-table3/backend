const express = require('express');
const cors = require('cors');
const { default: axios } = require('axios');

async function getSpotify(request, response, error) {
    try {
        let dataUrl= 'https://api.spotify.com/v1/search';

    } catch(error) {
        next(error);
    }
}